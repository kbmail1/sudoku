var assert = require('assert');
describe('Sudoku Data read', function() {
  describe('count read', function() {
    it('should be 2', function() {

      assert.equal([1, 2, 3].indexOf(4), -1);
    });
  });
});